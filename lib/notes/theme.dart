import 'package:flutter/material.dart';

// Current and new task elements ->
final deleteButtonStyle = ElevatedButton.styleFrom(primary: Colors.deepOrange);
const pressedBordersStyle = BorderSide(width: 0.5, color: Colors.black);
const unpressedBordersStyle = BorderSide(width: 0.1, color: Colors.black);

const defaultBackgroundColor = Color.fromRGBO(129, 222, 163, 1);
const greenBackgroundColor = Color.fromRGBO(129, 222, 163, 1);
const purpleBackgroundColor = Color.fromRGBO(204, 179, 255, 1);
const pinkBackgroundColor = Color.fromRGBO(255, 179, 179, 1);
const yellowBackgroundColor = Color.fromRGBO(255, 204, 128, 1);
const hintColor = Colors.grey;
const boxDecorationColor = Colors.indigo;



